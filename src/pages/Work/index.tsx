import Section from "@/components/base/Section";


export default function Work({ pageIndex }: { pageIndex: number; }) {
    return (
        <Section>
            <h1>Work</h1>
        </Section>
    );
}