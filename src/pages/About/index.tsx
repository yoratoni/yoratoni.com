import Section from "@/components/base/Section";


export default function About({ pageIndex }: { pageIndex: number; }) {

    return (
        <Section>
            <h1>About</h1>
        </Section>
    );
}