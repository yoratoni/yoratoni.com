import { IsWorkProject } from "@/types/general";


type ProjectProps = {
    key: number;
    project: IsWorkProject;
};

export default function Project({
    key,
    project
}: ProjectProps) {
    return (
        <a>
            
        </a>
    );
}